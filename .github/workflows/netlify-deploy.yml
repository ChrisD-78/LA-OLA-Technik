name: Deploy to Netlify

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
        
    - name: Install dependencies
      run: npm ci
      
    - name: Build project
      run: npm run build
      env:
        # Netlify will override these with values from netlify.toml
        REACT_APP_SUPABASE_URL: "https://toeskuoixedornrqrvzm.supabase.co"
        REACT_APP_SUPABASE_ANON_KEY: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InRvZXNrdW9peGVkb3JuZ3JydnptIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NTY5MjMwMzgsImV4cCI6MjA3MjQ5OTAzOH0.YOxx9UwFBwziOI81SJnIAeZI4wM71wkF3Y7e950CMlg"
        CI: false
        
    - name: Upload build artifacts
      uses: actions/upload-artifact@v4
      with:
        name: netlify-build-${{ github.sha }}
        path: build/
        retention-days: 30
        
    - name: Deploy success
      run: |
        echo "✅ Build successful!"
        echo "📦 Build artifacts uploaded"
        echo "🚀 Netlify will auto-deploy from GitHub integration"
        echo "🔗 Live URL: https://laola-technik.netlify.app"