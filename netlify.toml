[build]
  command = "npm run build"
  publish = "build"

# SPA redirect: serve index.html for all routes
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
